# ========================================================
# DEPENDENCIAS DE PYTHON (Instalar con: pip install -r requirements.txt)
# ========================================================
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
matplotlib==3.11.0
numpy==2.4.6
packaging==26.2
pillow==12.2.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0

# ========================================================
# REQUISITOS DEL SISTEMA OPERATIVO (Para la interfaz gráfica)
# ========================================================
# En Windows: No requiere nada adicional (Tkinter ya viene integrado).
# En macOS: No requiere nada adicional.
#
# En Ubuntu / Debian:
# sudo apt update && sudo apt install python3-tk
#
# En Kashi OS / Arch Linux:
# sudo pacman -S tk